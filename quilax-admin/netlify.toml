[build]
  publish = "dist"
  command = "npx expo export -p web"
  environment = { NODE_ENV = "production", APP_ENV = "production", EXPO_PUBLIC_API_URL = "https://api.appquilax.com" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
